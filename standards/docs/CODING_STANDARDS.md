<!-- gen: コーディング規約の雛形。「全PJ共通」節は原則そのまま維持し、「スタック固有」節を CLAUDE.md の技術スタックに合わせて具体化する。完成後 gen: コメントと {{ を全削除。 -->
# コーディング規約 — {{PROJECT_NAME}}

## 全PJ共通(Novexar 標準)
- **不変性**: 既存オブジェクトを破壊的変更せず、新しいコピーを返す。
- **小さなファイル/関数**: 関数 < 50 行、ファイル < 800 行(目安 200–400 行)、ネスト ≤ 4。
- **明示的なエラーハンドリング**: 握りつぶし禁止。境界(API 入力・DB・外部 API)で必ず入力検証。
- **秘密情報のハードコード禁止**: 環境変数 / Secrets 管理を使用。
- **デバッグ出力の残置禁止**: `console.log` / `print` のコミット禁止。
- **テスト**: TDD(RED→GREEN→REFACTOR)、カバレッジ 80%+、Unit / Integration / E2E の 3 層。

## スタック固有
### フロントエンド({{FE_STACK}})
<!-- gen: Lint/Formatter 設定、型の厳格度、ディレクトリ規約、状態管理方針を具体化 -->
- {{FE_STANDARDS}}

### バックエンド({{BE_STACK}})
<!-- gen: Lint/Formatter/型チェッカ、レイヤリング(router/service/repository)、非同期方針を具体化。BE 不在の PJ では本節を削除 -->
- {{BE_STANDARDS}}
