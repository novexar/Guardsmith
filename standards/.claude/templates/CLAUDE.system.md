<!-- ============================================================
  gen: モノレポ内の新システム用 CLAUDE.md 雛形(new-system スキルが使用)。
  1. {{PLACEHOLDER}} を確定情報で置換。不明点はPMに確認してから埋める。
  2. root CLAUDE.md と重複する内容は書かない(差分のみ)。
  3. 完成後、gen: コメントを全削除し、{{ が残っていないことを検証する。
============================================================ -->
# CLAUDE.md — {{SYSTEM_NAME}}

> root `/CLAUDE.md` を継承する。本書はシステム固有の差分のみ。

## システム概要
- **slug**: `{{system-slug}}`(ブランチ: `develop-{{system-slug}}` / ラベル: `system:{{system-slug}}`)
- **目的**: {{SYSTEM_PURPOSE_ONE_LINE}}

## 固有のコマンド差分
<!-- gen: root の「よく使うコマンド」と同じなら本節ごと削除 -->
| 目的 | コマンド |
|---|---|
| {{PURPOSE}} | {{COMMAND}} |

## 固有の特記事項
<!-- gen: このシステムだけのDB・外部連携・画面要件など。無ければ「特記事項なし」 -->
- {{SYSTEM_SPECIFIC_NOTES}}
